import React from "react";
// import "./index.css";

const Header = ({ title }) => {
  return (
    <div className="header bg-primary">
      <h1>{title}</h1>
    </div>
  );
};

export default Header;
